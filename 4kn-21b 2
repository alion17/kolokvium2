#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    int l;
    string s = get_string("Введіть строку: ");
    l = strlen(s);
    printf("Результат: ");
    for (int i = l; i >= 0; i--)
    {
        printf("%c", s[i]);
    }
}
