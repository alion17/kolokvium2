#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string s = get_string("Введіть строку: ");
    length = strlen(s);
    printf("Результат: ");
    for (int i = length; i > 0; i--)
    {
        printf("%d", s[i]);
    }
}
